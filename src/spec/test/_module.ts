/**
 * Spec module metadata for the `test` spec.
 *
 * Files prefixed with `_` describe the module rather than contributing targets:
 * the target-bearing loaders skip them, so this file does not export `spec`.
 *
 * `test` is isolated — it exists to exercise generators and views in a small,
 * fast slice, and never merges into the union dataset built from real
 * education standards.
 */
export const isolated = true;
