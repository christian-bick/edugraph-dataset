### Sorting Category Visual Checklist

#### Mode Instruction Rules:
- **Question Mode (`_mode-Q`):** Instruction headers (e.g. "Classify and count the objects by...", "Which category has the greatest number of items?") must be displayed above the sorting container.
- **Solution Mode (`_mode-S`):** Instruction text headers must **NOT** be displayed.
