# Classifying and Counting

- **Identity:** A collection and category rows make classifying by the stated visual trait and counting each category identifiable.
- **Modes:** Question Mode leaves every category count empty; Solution Mode reveals the counts matching the visible collection.
