import {createRoot} from 'react-dom/client'; import {ViewRenderPayload} from '../../../../types/ml-engine.ts'; import {withConfig} from '../../withConfig.tsx'; import {ShapeSquareArrayView} from '../shape-square-array-view.tsx'; import {ShapeSquareArrayStoryViewConfig, ShapeSquareArrayStoryViewSchema} from './spec.ts'; import '../../../../tailwind.css';
const VIEW_ID = 'shape-square-array-story'; interface CoreProps {config: ShapeSquareArrayStoryViewConfig; payload: ViewRenderPayload<'shape-square-array-story'>} const Core = ({payload}: CoreProps) => <ShapeSquareArrayView mode="execution" payload={payload} useStory viewId={VIEW_ID} />; export const ShapeSquareArrayStory = withConfig(ShapeSquareArrayStoryViewSchema, Core); let root: ReturnType<typeof createRoot> | null = null; window.renderView = (payload: ViewRenderPayload<'shape-square-array-story'>) => {const container = document.getElementById('view'); if (!container) return; if (!root) root = createRoot(container); root.render(<ShapeSquareArrayStory payload={payload} />);};
