import {createRoot} from 'react-dom/client'; import {ViewRenderPayload} from '../../../../types/ml-engine.ts'; import {withConfig} from '../../withConfig.tsx'; import {ShapePartitionView} from '../shape-partition-view.tsx';
import {ShapePartitionFractionInterpretationViewConfig, ShapePartitionFractionInterpretationViewSchema} from './spec.ts'; import '../../../../tailwind.css';
const VIEW_ID = 'shape-partition-fraction-interpretation'; interface CoreProps {config: ShapePartitionFractionInterpretationViewConfig; payload: ViewRenderPayload<'shape-partition-fraction-interpretation'>}
const Core = ({payload}: CoreProps) => <ShapePartitionView payload={payload} task="interpret-fraction" viewId={VIEW_ID} />; export const ShapePartitionFractionInterpretation = withConfig(ShapePartitionFractionInterpretationViewSchema, Core); let root: ReturnType<typeof createRoot> | null = null;
window.renderView = (payload: ViewRenderPayload<'shape-partition-fraction-interpretation'>) => {const container = document.getElementById('view'); if (!container) return; if (!root) root = createRoot(container); root.render(<ShapePartitionFractionInterpretation payload={payload} />);};
