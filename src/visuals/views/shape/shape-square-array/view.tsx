import {createRoot} from 'react-dom/client'; import {ViewRenderPayload} from '../../../../types/ml-engine.ts'; import {withConfig} from '../../withConfig.tsx'; import {ShapeSquareArrayView} from '../shape-square-array-view.tsx';
import {ShapeSquareArrayViewConfig, ShapeSquareArrayViewSchema} from './spec.ts'; import '../../../../tailwind.css';
const VIEW_ID = 'shape-square-array'; interface CoreProps {config: ShapeSquareArrayViewConfig; payload: ViewRenderPayload<'shape-square-array'>}
const Core = ({payload}: CoreProps) => <ShapeSquareArrayView mode="execution" payload={payload} useStory={false} viewId={VIEW_ID} />; export const ShapeSquareArray = withConfig(ShapeSquareArrayViewSchema, Core); let root: ReturnType<typeof createRoot> | null = null;
window.renderView = (payload: ViewRenderPayload<'shape-square-array'>) => {const container = document.getElementById('view'); if (!container) return; if (!root) root = createRoot(container); root.render(<ShapeSquareArray payload={payload} />);};
