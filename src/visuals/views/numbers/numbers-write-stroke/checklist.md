### Numbers Write Stroke Visual Checklist

- The tracing guide MUST include every digit of the target numeral, including all three digits for values from 100 through 120.
- The repeated practice boxes MUST be labeled `Trace` and `Write` so repeated zeroes cannot be mistaken for one multi-digit numeral.
