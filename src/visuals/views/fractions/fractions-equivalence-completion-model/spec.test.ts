import {Ability, Scope} from 'edugraph-ts'; import {describe, expect, it} from 'vitest'; import {spec} from './spec.ts'; describe('fractions-equivalence-completion-model view spec', () => {it('owns invariant equivalence formalization', () => {expect(spec.generalLabels).toEqual([Scope.VisualNumbers, Scope.SingleFrameOfReference, Ability.Formalization]); expect(spec.rejectedLabels).toBeUndefined();});});
