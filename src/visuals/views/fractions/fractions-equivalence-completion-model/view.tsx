import {createRoot} from 'react-dom/client'; import {ViewRenderPayload} from '../../../../types/ml-engine.ts'; import {withConfig} from '../../withConfig.tsx'; import {FractionEquivalenceView} from '../fraction-equivalence-view.tsx'; import {FractionsEquivalenceCompletionModelViewConfig, FractionsEquivalenceCompletionModelViewSchema} from './spec.ts'; import '../../../../tailwind.css';
const VIEW_ID = 'fractions-equivalence-completion-model'; interface CoreProps {config: FractionsEquivalenceCompletionModelViewConfig; payload: ViewRenderPayload<'fractions-equivalence-completion-model'>} const Core = ({payload}: CoreProps) => <FractionEquivalenceView mode="formalization" payload={payload} viewId={VIEW_ID} />; export const FractionsEquivalenceCompletionModel = withConfig(FractionsEquivalenceCompletionModelViewSchema, Core); let root: ReturnType<typeof createRoot> | null = null; window.renderView = (payload: ViewRenderPayload<'fractions-equivalence-completion-model'>) => {const container = document.getElementById('view'); if (!container) return; if (!root) root = createRoot(container); root.render(<FractionsEquivalenceCompletionModel payload={payload} />);};
