import {createRoot} from 'react-dom/client'; import {ViewRenderPayload} from '../../../../types/ml-engine.ts'; import {withConfig} from '../../withConfig.tsx'; import {FractionEquivalenceView} from '../fraction-equivalence-view.tsx'; import {FractionsEquivalenceModelViewConfig, FractionsEquivalenceModelViewSchema} from './spec.ts'; import '../../../../tailwind.css';
const VIEW_ID = 'fractions-equivalence-model'; interface CoreProps {config: FractionsEquivalenceModelViewConfig; payload: ViewRenderPayload<'fractions-equivalence-model'>} const Core = ({payload}: CoreProps) => <FractionEquivalenceView mode="classification" payload={payload} viewId={VIEW_ID} />; export const FractionsEquivalenceModel = withConfig(FractionsEquivalenceModelViewSchema, Core); let root: ReturnType<typeof createRoot> | null = null; window.renderView = (payload: ViewRenderPayload<'fractions-equivalence-model'>) => {const container = document.getElementById('view'); if (!container) return; if (!root) root = createRoot(container); root.render(<FractionsEquivalenceModel payload={payload} />);};
