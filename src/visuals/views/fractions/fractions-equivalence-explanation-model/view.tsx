import {createRoot} from 'react-dom/client'; import {ViewRenderPayload} from '../../../../types/ml-engine.ts'; import {withConfig} from '../../withConfig.tsx'; import {FractionEquivalenceView} from '../fraction-equivalence-view.tsx'; import {FractionsEquivalenceExplanationModelViewConfig, FractionsEquivalenceExplanationModelViewSchema} from './spec.ts'; import '../../../../tailwind.css';
const VIEW_ID = 'fractions-equivalence-explanation-model'; interface CoreProps {config: FractionsEquivalenceExplanationModelViewConfig; payload: ViewRenderPayload<'fractions-equivalence-explanation-model'>} const Core = ({payload}: CoreProps) => <FractionEquivalenceView mode="explanation" payload={payload} viewId={VIEW_ID} />; export const FractionsEquivalenceExplanationModel = withConfig(FractionsEquivalenceExplanationModelViewSchema, Core); let root: ReturnType<typeof createRoot> | null = null; window.renderView = (payload: ViewRenderPayload<'fractions-equivalence-explanation-model'>) => {const container = document.getElementById('view'); if (!container) return; if (!root) root = createRoot(container); root.render(<FractionsEquivalenceExplanationModel payload={payload} />);};
