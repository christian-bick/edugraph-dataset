import {Ability, Scope} from 'edugraph-ts'; import {describe, expect, it} from 'vitest'; import {spec} from './spec.ts'; describe('fractions-equivalence-explanation-model view spec', () => {it('owns invariant scaling explanation', () => {expect(spec.generalLabels).toEqual([Scope.VisualNumbers, Scope.SingleFrameOfReference, Ability.Formalization, Ability.ProcedureUnderstanding]); expect(spec.rejectedLabels).toBeUndefined();});});
