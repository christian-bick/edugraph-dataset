### Writing Checklist

- The problem data must define a valid target number to write, trace, or represent within the configured range.
- When the resolved ontology range extends beyond grade 1, generated values MUST still remain at or below 120.
- A high-range `NumbersWithoutZero` target MUST use 111–119 so every displayed place is nonzero.
